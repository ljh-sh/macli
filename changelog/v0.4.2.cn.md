---
version: v0.4.2
date: 2026-06-20
---

# macli v0.4.2

## 发布渠道

- 修复 npm postinstall 脚本：改用 Node.js 内置的 `fetch` 下载二进制，不再依赖系统 `curl`。
- 解决部分 macOS 环境（如 x-cmd 的 curl shim）下 `npm install -g @ljh-sh/macli` 失败的问题。
- 下载后校验 GitHub release 的 SHA256SUMS。

CLI 功能无变化。
